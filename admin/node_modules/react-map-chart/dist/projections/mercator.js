'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
var TILE_SIZE = 256;
var pixelOrigin = [TILE_SIZE / 2, TILE_SIZE / 2];
var pixelsPerLonDegree = TILE_SIZE / 360;
var pixelsPerLonRadian = TILE_SIZE / (2 * Math.PI);

function bound(value, optMin, optMax) {
  value = Math.max(value, optMin);
  value = Math.min(value, optMax);
  return value;
}

var degreesToRadians = function degreesToRadians(deg) {
  return deg * .017453292519943295;
};

var mercator = exports.mercator = {
  projection: function projection(latLng) {
    var siny = bound(Math.sin(degreesToRadians(latLng[1])), -0.9999, 0.9999);
    return [pixelOrigin[0] + latLng[0] * pixelsPerLonDegree, pixelOrigin[1] + 0.5 * Math.log((1 + siny) / (1 - siny)) * -pixelsPerLonRadian];
  },
  viewBox: '0 8 266 190'
};